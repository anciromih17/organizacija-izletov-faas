rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /trip-documents/{registrationId}/{fileName} {
      // Naloži samo prijavljen user
      allow write: if request.auth != null;
      // Bere lastnik ali admin
      allow read: if request.auth != null;
    }
  }
}